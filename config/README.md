# Configuration

Store non-secret settings here, such as input filenames, date ranges, field mappings, geographic filters, and output options.

Never commit API keys, passwords, service-account files, or private download links. Put secrets in environment variables and document only their variable names in an `.env.example` file.
